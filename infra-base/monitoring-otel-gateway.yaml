apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: monitoring-otel-gateway
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - name: dev-demo
        server: https://kubernetes.default.svc
        revision: 0.142.1

  template:
    metadata:
      name: 'monitoring-otel-gateway-{{name}}'
    spec:
      project: hands-on
      source:
        repoURL: 'https://open-telemetry.github.io/opentelemetry-helm-charts'
        chart: opentelemetry-collector
        targetRevision: '{{revision}}'
        helm:
          valuesObject:
            mode: deployment
            fullnameOverride: "otel-gateway"
            replicaCount: 1

            image:
              repository: "otel/opentelemetry-collector-contrib"

            presets:
              kubernetesAttributes:
                enabled: true

            resources:
              requests:
                memory: 1Gi
                cpu: 500m
              limits:
                memory: 2Gi
                cpu: 1000m

            config:
              receivers:
                otlp:
                  protocols:
                    grpc:
                      endpoint: ${env:MY_POD_IP}:4317
                    http:
                      endpoint: ${env:MY_POD_IP}:4318

              processors:
                batch: {}

                memory_limiter:
                  check_interval: 5s
                  limit_percentage: 80
                  spike_limit_percentage: 25

                resource:
                  attributes:
                    - key: loki.resource.labels
                      value: k8s.namespace.name, k8s.pod.name, k8s.container.name, k8s.node.name, service.name
                      action: insert

                attributes:
                  actions:
                    - key: namespace
                      from_attribute: k8s.namespace.name
                      action: upsert
                    - key: pod
                      from_attribute: k8s.pod.name
                      action: upsert
                    - key: container
                      from_attribute: k8s.container.name
                      action: upsert
                    - key: node_name
                      from_attribute: k8s.node.name
                      action: upsert

                tail_sampling:
                  policies:
                    - name: errors
                      type: status_code
                      status_code:
                        status_codes: [ERROR]
                    - name: slow-traces
                      type: latency
                      latency:
                        threshold_ms: 1000
                    - name: probabilistic-sample
                      type: probabilistic
                      probabilistic:
                        sampling_percentage: 10

              exporters:
                otlphttp/loki:
                  endpoint: http://monitoring-loki-dev-demo.monitoring.svc:3100/otlp
                  tls:
                    insecure: true

                otlp/tempo:
                  endpoint: "monitoring-tempo-dev-demo.monitoring.svc:4317"
                  tls:
                    insecure: true

                prometheusremotewrite:
                  endpoint: http://monitoring-base-dev-demo-kube-prom-prometheus.monitoring.svc:9090/api/v1/write
                  resource_to_telemetry_conversion:
                    enabled: true

              service:
                pipelines:
                  logs:
                    receivers: [otlp]
                    processors: [memory_limiter, resource, attributes, batch]
                    exporters: [otlphttp/loki]
                  metrics:
                    receivers: [otlp]
                    processors: [memory_limiter, k8sattributes, batch]
                    exporters: [prometheusremotewrite]
                  traces:
                    receivers: [otlp]
                    processors: [memory_limiter, k8sattributes, tail_sampling, batch]
                    exporters: [otlp/tempo]

            clusterRole:
              create: true

      destination:
        server: '{{server}}'
        namespace: monitoring
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
