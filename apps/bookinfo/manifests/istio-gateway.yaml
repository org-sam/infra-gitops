apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: reviews
  namespace: bookinfo
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - bookinfo.mkt.posanhanguera.com.br
